import type { FileInfo, Imports, Backend } from './types';
declare function makeMCRCourseApp({ imports, filesInfo, projectTitle, backend, options, ...otherWorkshopOptions }: {
    imports: Imports;
    filesInfo: Array<FileInfo>;
    projectTitle: string;
    backend?: Backend;
    options?: {
        concurrentMode?: boolean;
    };
} & {
    gitHubRepoUrl: string;
}): void;
export { makeMCRCourseApp };
