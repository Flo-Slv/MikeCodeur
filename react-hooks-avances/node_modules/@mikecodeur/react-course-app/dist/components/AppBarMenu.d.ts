/** @jsx jsx */
/** @jsxFrag React.Fragment */
import React from 'react';
import { DarkModeState, SetDarkModeState } from 'types';
import type { History } from 'history';
import type { ExerciseInfo } from '../types';
export default function AppBarMenu({ exerciseNumber, mode, setMode, exerciseInfo, classes, history, theme, projectTitle, }: {
    exerciseNumber?: number;
    mode: DarkModeState;
    setMode: SetDarkModeState;
    exerciseInfo: Array<ExerciseInfo>;
    classes: any;
    history: History;
    theme: any;
    projectTitle: string;
}): React.ReactElement;
